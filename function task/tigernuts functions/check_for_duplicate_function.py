def check_for_duplicate(numbers):
	count = 0
	for duplicate in range numbers:
		count = 0
		for check in range numbers:
			
			if duplicate == range :
				count+=1
			if count >= 2 :
				return True
				break
	if count < 2 :	False 
			