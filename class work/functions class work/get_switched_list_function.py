def get_switched_list(numbers:list,element):
	switch1 = numbers[:element:] 
	switch2 = numbers[element::] 
	return switch2+switch1

#def switch_int(numbers: list, number:int):
	#for _ in number[0]
	each = number[0]
	number.remove(number[o])
	numbers.append(each)
	return numbers